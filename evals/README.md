# Evaluation suites

This directory is the versioned home for extraction, retrieval, permission, and
agent evaluation suites. M0 contains no synthetic pass result or placeholder
score; each later suite must commit legal fixtures, deterministic expectations,
and a documented runner before its milestone can claim coverage.

Skipped, mock-only, or manually inferred results are not evaluation receipts.
