# Runtime secret mount point

Files in this directory are generated locally or provisioned by a secret manager and are ignored by Git. Run `../scripts/bootstrap-secrets.sh` from anywhere to create development values with owner-only permissions. Do not add real values to this README, Compose, image layers, `.env`, logs, or test fixtures.
