# PcbKnowledge 内部介绍

## 一句话

PcbKnowledge 把 PCB 资料整理工作变成一次容易审阅的 Git 变更：Agent 先准备，人来确认，Git
负责记录。

## 为什么这样设计

当前实际使用者只有内部工程师和产品经理。早期在线平台虽然具备数据库、登录、权限与对象存储，
但克隆后需要长时间构建、启动多项服务并输入密码，远重于“录入几份资料”的任务。

当前版本把权威数据移回仓库：

```text
一条资料记录 = 一份可读 JSON
一份原件      = 一份按 SHA-256 命名的 PDF
一次交付      = 一次人确认过的 Git commit
```

这样工程师和产品经理都可以在 GUI 中操作，不需要理解数据库或 Docker；Agent 也能直接编辑同一
套文件，不需要管理 token。

## 每个人做什么

### AI Agent

- 找到候选资料并按稳定 idempotency key 建草稿；
- 填写已经证实的标题、版本、发布者、来源与许可；
- 导入原件、报告缺失字段并提交人工审阅；
- 不猜未知项，不批准，不提交 Git。

### PCB 工程师 / 产品经理

- 打开 FreeCM Run 自动弹出的工作台；
- 核对来源、版本、许可和 PDF；
- 补充、退回或批准；
- 在“仓库变化”页或 Git GUI 检查 diff；
- 决定何时 commit/push。

产品经理可以准备与退回资料；对高风险工程事实的批准责任仍应由具备相应判断能力的人承担。当前
代码只实现资料级批准，后续细分知识事实时会再建立更严格审阅规则。

## 两个人怎样协作

每个人使用自己的 clone。开始工作前拉取最新 main；完成一小批记录后查看 diff、提交并推送。另一
个人拉取时，普通 JSON 冲突由 Git 明确呈现，不会被数据库中的最后一次写入静默覆盖。

Git 作者和提交说明记录“谁在何时接受了这批变化”。这对当前可信小团队足够；如果将来需要同时
在线编辑、企业 SSO 或不可否认审计，再建设共享服务。

## 出错后怎么办

- 未保存：浏览器页面没有改变仓库。
- 已保存未提交：Git GUI 会显示全部变化，可逐文件检查或撤销。
- 已提交：通过新 commit 修正；已批准记录必须新增 superseding 记录，不能改旧事实。
- 电脑损坏：从 Git remote 恢复已 push 的数据；尚未提交/推送的资料需要工作站备份。

## 与 PcbCore 的关系

PcbKnowledge 保存外部资料与证据。PcbCore 保存当前板卡的几何、连接与确定性验证。Agent 可以同时
参考两边，但 PcbKnowledge 永远不修改 PCB，PcbCore 也不依赖本工具才能打开或验证板卡。
