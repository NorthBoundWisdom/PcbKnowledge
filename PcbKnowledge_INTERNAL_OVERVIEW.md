# PcbKnowledge 内部介绍：它解决什么问题，以及如何进入 PCB 软件全流程

> 文档状态：内部科普与项目介绍  
> 文档版本：1.0  
> 日期：2026-08-07  
> 适用读者：产品、测试/数据运营、电子工程师、PCB 设计人员、前后端工程师、Agent 工程师、管理与审计人员  
> 关联架构：`PcbKnowledge_ARCHITECTURE.md`  
> 说明：本文用于解释项目价值、使用方式和业务边界；具体技术选型、数据库设计、API 和部署规范以正式架构文档为准。

---

## 1. 一句话说明

`PcbKnowledge` 是 PCB 软件和 PCB Agent 共用的**工程知识与证据平台**。

它把散落在 datasheet、application note、reference design、板厂能力文件、内部规范、design review、waiver、PCN 和历史工程中的知识，整理为：

```text
有明确对象
+ 有适用条件
+ 有文档版本
+ 有原文证据
+ 有人工审核
+ 可被软件精确查询
+ 可被 Agent 安全使用
```

它最终解决的不是“让 AI 更会聊天”，而是让 PCB 软件和 Agent 在做工程判断时能够回答：

- 这个结论来自哪里？
- 对应哪一个器件、封装和版本？
- 在什么条件下成立？
- 当前使用的是不是最新有效版本？
- 是否经过工程师审核？
- 有没有相互冲突的资料？
- 这个建议能否被 PcbCore 和验证流程证明可行？

---

## 2. 为什么需要单独建设这套系统

### 2.1 PCB 工程知识目前通常是分散的

同一个设计问题可能需要同时查阅：

- 元器件 datasheet；
- application note；
- reference design；
- 厂商布局指南；
- 板厂工艺能力；
- 公司内部规范；
- 历史项目的 design review；
- 已批准的 waiver；
- PCN、停产通知和替代料记录。

这些内容往往分散在个人电脑、网盘、邮件、项目目录、网页和工程师经验中。即使材料已经存在，也不一定能在设计发生的那一刻被正确找到和使用。

### 2.2 大模型不能被当作工程事实库

通用模型可以解释概念、阅读文档和提出建议，但不能保证：

- 记住的是当前 revision；
- 没有混淆同系列不同 MPN；
- 没有混淆不同 package 的 pin 定义；
- 能区分 absolute maximum 与 recommended operating condition；
- 能区分厂商建议、公司规则和项目 waiver；
- 不会用相似器件的资料填补未知内容。

因此，模型只能参与抽取、检索、解释和推理，不能独自发布高风险工程事实。

### 2.3 普通文档搜索也不够

搜索出一段“看起来相关”的文字，并不等于得到了可以用于工程决策的知识。

例如：

```text
“VIN 最大为 6 V”
```

至少还需要知道：

- 哪个具体器件？
- 哪个封装或 silicon revision？
- 这是绝对最大值还是推荐工作范围？
- 是否有温度、时间或测试条件？
- 来自哪一版 datasheet 的哪一页、哪一行？
- 后续 revision 是否已经修改？
- 是否经过工程师审核？

`PcbKnowledge` 的价值，就是把“搜到一段文字”提升为“获得一条可以追溯和审核的工程记录”。

### 2.4 PCB Agent 需要稳定、可复现的上下文

Agent 可能在几分钟内完成一次设计审查，也可能跨多轮完成器件替换、布局优化、布线和验证。如果知识库在过程中不断变化，而任务没有记录所用版本，就无法解释结果，也无法做回归测试。

因此系统会为正式任务创建 `KnowledgeSnapshot`。它类似软件依赖的 lockfile，记录本次任务实际使用了哪些文档版本、知识记录和检索策略。

---

## 3. 它与现有 PCB 软件各部分是什么关系

可以把整个系统理解为四个彼此分工的角色：

```text
PcbCore
= 当前板卡的事实、几何、连接、规则执行和确定性验证

PcbKnowledge
= 外部工程知识、原文证据、内部规则和历史经验

PcbAgentHarness
= 理解任务、组合上下文、调用工具、生成修改提案并推进闭环

Human Approval
= 对高风险工程决策进行最终授权
```

### 3.1 PcbCore 回答“板子现在是什么样”

例如：

- U23 当前位于哪里；
- 某个 pin 连接到哪个 net；
- 两段铜之间的实际间距；
- 差分对长度和 skew；
- 当前规则命中了哪些对象；
- 某个 `BoardPatch` 应用后是否通过 ERC、DRC 或 DFM。

这些信息必须来自精确数据结构和确定性算法，不使用 RAG 判断。

### 3.2 PcbKnowledge 回答“设计时应该参考什么”

例如：

- 这个器件的 VIN 推荐范围是什么；
- 哪些 pin 具有 alternate function；
- 电源上电顺序有什么要求；
- 去耦电容的容量、数量和摆放有什么厂商建议；
- 晶振和复位电路有哪些条件；
- 某板厂当前工艺 profile 的最小线宽/间距是多少；
- 当前项目有哪些已批准 waiver；
- 该料号是否收到 PCN 或进入停产阶段；
- 以前是否处理过相似问题。

### 3.3 Agent Harness 负责把两类信息组合起来

典型调用过程是：

```text
用户提出目标
→ Harness 从 PcbCore 获取当前 BoardContext
→ 根据 MPN、package、项目和板版本查询 PcbKnowledge
→ 获得带证据的 EvidenceBundle
→ Agent 生成设计建议或 BoardPatch
→ PcbCore 执行确定性验证
→ 人工审批
→ 应用、拒绝或回滚
```

### 3.4 PcbKnowledge 不能成为 PcbCore 的硬依赖

即使知识平台暂时离线，软件仍必须能够：

- 打开和保存 PCB；
- 编辑对象；
- 执行布局和布线；
- 运行现有 ERC、DRC 和 DFM；
- 查看当前板卡状态。

知识服务不可用时，受影响的是智能建议、外部知识查询和证据展示，而不是 PCB 编辑内核。

---

## 4. 它保存的到底是什么

系统不把所有内容都视为“文本块”。正式知识至少分为以下六类：

| 类型 | 含义 | 典型内容 |
|---|---|---|
| `Fact` | 可精确查询的工程事实 | pin、额定值、电压范围、时序参数 |
| `Rule` | 应执行或应参考的规则 | 去耦、布局、板厂工艺、内部设计规范 |
| `Decision` | 人工做出的工程决定 | design review 结论、设计取舍 |
| `Waiver` | 在限定范围内批准的例外 | 某项目、某板版本、某规则的豁免 |
| `Case` | 可复用的成功或失败案例 | reference design、历史修复、失败 patch |
| `Lifecycle` | 与时间和版本相关的信息 | PCN、停产、替代料、revision 迁移 |

每条正式记录都需要尽可能包含：

```text
作用对象
+ 数值或规则内容
+ 单位
+ 条件
+ 适用范围
+ 来源权威等级
+ 文档 revision
+ 原文页码与区域
+ 抽取置信度
+ 人工审核状态
+ 生效和失效时间
+ 冲突、替代和修订关系
```

### 4.1 系统允许明确回答“不知道”

以下都是正常结果：

- 已找到；
- 存在冲突；
- 不适用；
- 未知；
- 无权访问；
- 数据已过期。

系统不会为了“给出一个答案”而使用同系列器件、相似封装或模型常识补全缺失事实。

---

## 5. 数据是怎样进入系统的

整体流程分为“知识生产”和“知识使用”两部分。

### 5.1 知识生产流程

```text
原始资料上传
→ 记录来源、许可证、版本和权限
→ 保存不可变原文件
→ 解析 PDF 页面、文本、表格和图形区域
→ 识别 Manufacturer / MPN / Package 等实体
→ 抽取结构化候选记录
→ 测试/数据人员核对证据
→ 电子工程师审核高风险内容
→ 发布为正式知识
→ 建立搜索索引和评测用例
```

### 5.2 知识使用流程

```text
人或 Agent 发起精确查询
→ 先按项目、权限、时间和器件范围过滤
→ 优先读取已审核的结构化记录
→ 必要时再查原文和相似案例
→ 检查冲突、缺失和过期状态
→ 返回 EvidenceBundle
→ 在软件中展示或进入 Agent 工作流
```

### 5.3 永久资产与可重建资产

长期真正有价值的是：

```text
原始文档
+ 版本元数据
+ EvidenceAnchor
+ 结构化知识
+ 人工审核记录
+ 冲突和替代关系
+ KnowledgeSnapshot
+ Golden Eval
```

以下内容可以在算法或模型升级后重新生成：

```text
文本切块
+ 全文索引
+ embedding
+ vector index
+ reranker cache
+ 自动摘要
```

这意味着项目可以先开始积累资料和审核数据，而不必等待“最好的 RAG 算法”确定后再开工。

---

## 6. 内部人员会看到哪些软件界面

不是所有人都会直接使用同一个界面。系统主要有三种软件入口。

### 6.1 Curator Web：知识录入和审核后台

这是测试/数据运营人员、电子工程师和知识管理员直接使用的桌面 Web 工作台。

#### Dashboard

用于查看：

- 待处理文档；
- 待审核记录；
- 有冲突或已过期的知识；
- 解析和抽取任务状态；
- 检索、抽取和评测回归情况。

#### Intake Inbox 与上传向导

用于：

- 上传 datasheet、app note、reference design 等资料；
- 选择来源类型和许可证策略；
- 填写文档编号、revision、发布日期；
- 绑定 manufacturer、MPN、package 和项目范围；
- 提交解析任务。

#### Document Library

用于：

- 查找文档及其历史 revision；
- 查看原始文件 hash、来源和访问范围；
- 对比当前版本和旧版本；
- 查看从文档中抽取了哪些知识记录。

#### Review Workbench

这是最核心的审核界面，固定由四个区域组成：

```text
左侧：页面缩略图和状态
中间：原始 PDF 与证据区域高亮
右侧：结构化字段、条件、单位和审核动作
底部：原文、历史、冲突和审计记录
```

审核人员可以点击字段直接跳转到原文，也可以框选 PDF 区域建立 `EvidenceAnchor`。

#### Entity Resolver

用于解决名称和对象混淆，例如：

- family 与具体 orderable part；
- 相似但不同的 MPN；
- package code 与实际封装；
- 同一器件的不同 silicon revision；
- 内部料号与厂商料号映射。

#### Knowledge Explorer

用于查看：

- 已发布事实和规则；
- 来源证据；
- 版本链和替代关系；
- 冲突记录；
- 哪些 Agent 任务或快照引用过该知识；
- 是否需要创建修订版本。

已发布内容不能被直接覆盖，只能创建新版本。

#### Conflict Center

用于处理：

- 新旧 datasheet 数值不同；
- datasheet 与 app note 表述不同；
- 厂商资料与内部规则不同；
- 不同板厂 profile 的工艺限制不同；
- 同一器件不同封装或 revision 的定义被错误合并。

系统不会静默选择一个结果并隐藏其他结果。

#### Eval Center 与 Audit Explorer

用于：

- 维护 golden cases；
- 运行抽取和检索回归；
- 查看版本升级是否引入退化；
- 追踪谁在什么时间上传、修改、审核、发布或查询了某条记录。

---

### 6.2 PCBAtlas 或其他 PCB 客户端：设计人员实际感知到的能力

PCB 设计人员通常不需要进入知识治理后台，而是在现有软件中看到知识服务提供的上下文能力。

可能的 UI 形态包括：

#### 元件知识侧栏

用户选中一个元件后，可查看：

- 精确 MPN 和 package；
- pin function 与 alternate function；
- 推荐工作条件和绝对最大值；
- 电源、时钟、复位和去耦要求；
- 生命周期、PCN 和替代料状态；
- 每条信息对应的文档 revision、页码和原文预览。

#### 证据卡片

任何 Agent 建议都可以附带证据卡片，例如：

```text
建议：在 VDD 与 GND 之间增加 100 nF 去耦电容
适用对象：U23 / TPSxxxx / WSON-6
来源：Datasheet Rev. J，第 18 页，Power Supply Recommendations
状态：工程师已审核
```

用户可以直接打开原文，而不是只看到一句无法验证的 AI 结论。

#### 设计规则说明

当软件提示规则或风险时，可以同时说明：

- 这是 PcbCore 的硬性 DRC；
- 这是板厂工艺 profile 的限制；
- 这是厂商推荐；
- 这是公司内部规则；
- 这是某项目已批准的 waiver；
- 还是仅供参考的历史案例。

这些类型必须在 UI 中视觉上区分，避免用户把“建议”误认为“硬性错误”。

#### 设计审查报告

软件可以生成带证据的 review 报告：

- 当前发现的问题；
- 使用了哪些规则和资料；
- 哪些结论存在冲突或信息缺失；
- 哪些修改已经通过 PcbCore 验证；
- 哪些项目仍需人工审批；
- 本次使用的 `KnowledgeSnapshot`。

#### 文档更新和影响提示

当新的 datasheet、PCN 或板厂能力发布后，软件可提示：

- 哪些知识记录被替代；
- 哪些历史 Agent 任务使用过旧版本；
- 哪些项目可能需要重新检查；
- 哪些影响只是信息更新，哪些可能触发 ECO。

系统只生成影响报告，不自动修改板卡。

---

### 6.3 PcbAgentHarness：Agent 的知识工具层

Agent 不应直接把自然语言问题扔给一个无限制的“文档聊天接口”。它会调用明确的领域接口，例如：

```text
resolve_component
get_pin_spec
get_alternate_functions
get_limits
get_power_sequence
get_decoupling_requirements
get_clock_reset_requirements
get_layout_guidelines
get_reference_circuits
get_fab_capabilities
get_internal_rules
get_waivers
get_lifecycle
get_replacements
find_similar_cases
search_evidence
```

返回结果不是一段自由文本，而是结构化的 `EvidenceBundle`：

```text
硬性约束
+ 工程建议
+ 已批准例外
+ 参考案例
+ 冲突
+ 缺失信息
+ 原文证据
+ KnowledgeSnapshot ID
```

这样 Agent 可以据此制定计划，但不能绕过 PcbCore 的验证或人工审批。

---

## 7. PCB 全流程中可能用到的场景

下表说明 PcbKnowledge 在各阶段可能提供什么能力，以及它不负责什么。

| 设计阶段 | 典型问题 | PcbKnowledge 的作用 | 最终裁判或执行者 |
|---|---|---|---|
| 项目建立 | 本项目采用什么板厂、工艺 profile 和内部规范 | 提供当前有效规则、来源和版本 | 项目负责人、规则系统 |
| 器件选型 | 器件是否适合、是否停产、是否有替代料 | 提供 datasheet、生命周期、PCN 和候选替代证据 | 电子工程师、采购/供应链流程 |
| 原理图设计 | pin、alternate function、电源时序、去耦、时钟和复位要求 | 提供结构化器件知识和原文依据 | ERC、仿真、电子工程师 |
| 库创建 | symbol、pin mapping、package 和机械信息来自哪里 | 提供经过核验的对象和证据 | 库校验器、电子工程师 |
| 布局规划 | 哪些器件需要靠近、哪些区域需要隔离 | 提供厂商布局建议、内部规则和历史案例 | Placement evaluator、DRC、工程师 |
| 自动布局 | 某个约束或目标依据是什么 | 提供约束来源和适用范围 | 优化器、PcbCore、人工审批 |
| 布线 | 高速、电源、模拟接口有哪些专用建议 | 提供接口指南和参考设计 | Router、DRC、SI/PI 工具 |
| 设计审查 | 设计是否遗漏厂商建议、内部规则或历史问题 | 生成带证据的审查项 | PcbCore 验证、工程师签核 |
| DRC/DFM | 当前板厂能力是否允许该结构 | 提供厂商和工艺 profile 的有效限制 | DRC/DFM 引擎 |
| Waiver | 某条违规是否已被批准例外 | 按项目、板版本和对象范围提供批准记录 | 审批流程和规则引擎 |
| 制造准备 | 换板厂或工艺后哪些规则变化 | 比较 capability profile 和版本 | DFM、制造工程师 |
| ECO | 新 datasheet、PCN 或替代料会影响什么 | 做知识差异和历史引用影响分析 | ECO 流程、工程师 |
| 故障分析 | 是否有相似 review、失败案例或已知限制 | 检索相关证据和历史案例 | 测试、分析工具、工程师 |
| Agent 长任务 | 如何保证任务中使用的知识不漂移 | 固定 KnowledgeSnapshot 并记录检索轨迹 | Harness 和回归系统 |
| 回归评测 | 模型或检索升级后是否变差 | 运行 golden extraction/retrieval cases | 测试与发布门禁 |

---

## 8. 六个具体使用示例

### 8.1 选中元件后查看可信器件信息

#### 用户操作

PCB 设计人员在 PCBAtlas 中选中 U23。

#### 软件流程

```text
PcbCore 返回：U23 的 MPN、package 和当前连接
→ PcbKnowledge 精确匹配器件实体
→ 返回 pin、工作条件、去耦和布局记录
→ UI 显示结构化信息和 PDF 原文证据
```

#### 用户价值

用户不需要在浏览器中反复查 PDF，也能知道当前显示的是哪个 revision、哪个 package，并可随时核对原文。

---

### 8.2 Agent 检查去耦设计

#### 用户目标

“检查 U23 的去耦是否满足厂商建议。”

#### 软件流程

```text
Harness 从 PcbCore 获取 U23、电源 pin、周围器件和几何
→ 查询 PcbKnowledge 的去耦要求
→ 得到容量、数量、适用条件和原文证据
→ Agent 对照当前 BoardContext 识别缺失项
→ 生成增加或移动电容的 BoardPatch 提案
→ PcbCore 验证连接、间距和制造规则
→ 人工批准后应用
```

#### 边界

PcbKnowledge 提供“厂商要求是什么”；PcbCore 判断“当前板上是否满足”和“修改后是否几何合法”。

---

### 8.3 更换板厂或制造工艺

#### 用户目标

“把当前项目从板厂 A 的普通四层工艺迁移到板厂 B 的高密度工艺。”

#### 软件流程

```text
查询两个 fab capability profile
→ 对比最小线宽、间距、孔径、环宽、材料和特殊工艺条件
→ 识别当前项目可能受影响的规则
→ PcbCore/DFM 使用新 profile 重新检查
→ 输出差异和整改清单
```

#### 用户价值

不再依赖工程师手工对照两份能力表，也不会把不同日期或不同订单等级的工艺条件混在一起。

---

### 8.4 处理项目 waiver

#### 场景

某个特殊区域无法满足公司默认间距，但已经由负责人批准例外。

#### 软件流程

```text
PcbCore 报告规则命中
→ Harness 按 project_id、board_revision_id、rule_id 和对象范围查询 waiver
→ PcbKnowledge 返回批准人、理由、有效期和证据
→ UI 将该问题显示为“已批准例外”而非普通通过
```

#### 边界

Waiver 不能跨项目、跨板版本或跨对象自动复用，也不能因为历史项目曾经批准过就推断当前项目同样有效。

---

### 8.5 新 datasheet 或 PCN 发布后的影响分析

#### 场景

器件厂商发布新 revision，修改了某个推荐工作条件或封装说明。

#### 软件流程

```text
新文档进入系统
→ 建立新旧 revision 差异
→ 生成新的知识记录版本
→ 标记旧记录被 supersede
→ 查找使用旧记录的 KnowledgeSnapshot、Agent run 和历史项目
→ 生成潜在影响报告
```

#### 用户价值

项目团队能知道“哪些设计可能需要重新检查”，而不是等问题在制造或现场阶段暴露。

---

### 8.6 从历史成功和失败中复用经验

#### 场景

Agent 正在处理一种复杂 BGA escape 或电源布局问题。

#### 软件流程

```text
按器件类别、网络类型、板层和问题特征查找相似 Case
→ 同时返回成功方案和失败方案
→ 显示当时的验证结果、人工结论和使用的知识快照
→ Agent 将其作为参考，而不是直接复制
```

#### 用户价值

工程经验不再只存在于个人记忆或最终板文件中，失败尝试也能成为可检索资产。

---

## 9. 不同岗位如何参与

### 9.1 测试/数据运营人员

主要负责把原始资料变成可审核、可检索的数据资产：

- 上传和分类文件；
- 填写来源、revision、日期和许可证；
- 绑定 MPN、family、package 等候选实体；
- 检查 PDF 页数和解析质量；
- 修正表格、文本和单位错误；
- 创建原文证据锚点；
- 核对模型抽取是否来自原文；
- 标记冲突和缺失；
- 建立 golden cases；
- 运行回归并分类失败。

他们不负责最终批准高风险电气事实。

### 9.2 电子工程师与领域审核员

主要负责：

- 审核 pin、alternate function 和电气极限；
- 确认电源时序、去耦、时钟和复位要求；
- 审核高速、模拟、电源和制造规则；
- 判断资料适用范围；
- 裁决冲突；
- 审批 waiver；
- 评估替代料兼容性；
- 设计高价值 golden cases。

### 9.3 PCB 设计人员

主要通过 PCB 客户端使用结果：

- 查看器件知识和证据；
- 接收设计建议和风险提示；
- 查看规则来源与 waiver；
- 审查 Agent 修改；
- 参与最终设计批准。

通常不需要直接维护底层知识记录。

### 9.4 前后端与平台工程师

主要负责：

- 文档、实体、知识、审核和权限的数据模型；
- Curator Web 和 Review Workbench；
- 解析、抽取、搜索、快照和审计服务；
- PcbCore 与 PCB 客户端适配；
- 备份、恢复、可观测性和安全隔离；
- 保证索引可重建、记录可迁移、任务可复现。

### 9.5 Agent 工程师

主要负责：

- 定义 Agent 在什么阶段查询什么知识；
- 优先使用 typed API，而不是无限制自由问答；
- 控制查询预算和原始页面读取范围；
- 把 EvidenceBundle 与 BoardContext 组合；
- 确保修改进入 PcbCore 验证；
- 保存 snapshot、检索轨迹和 Agent run；
- 将经过验证的成功和失败结果回写为 Case 候选。

### 9.6 产品、管理、合规与审计人员

主要关注：

- 哪些场景优先进入软件；
- 哪些资料允许被解析、embedding 或发送到外部模型；
- 数据是否跨项目泄漏；
- 高风险事实是否经过正确审批；
- 重要任务是否可复现；
- 系统带来的审核效率、质量和风险变化。

---

## 10. 第一阶段为什么不需要先做“完整 AI”

P0 的目标不是立即实现自动设计，而是先建立可持续的数据生产线：

```text
上传
+ 原始文件不可变保存
+ 文档版本
+ 实体映射
+ EvidenceAnchor
+ 人工审核
+ Published Knowledge
+ 精确查询
+ 全文搜索
+ 审计
+ Golden Eval
```

即使没有 embedding 和 LLM 自动抽取，这些工作也已经能够：

- 让测试人员开始整理首批数据；
- 让工程师形成统一审核口径；
- 让软件具备可引用的器件和规则查询；
- 为后续模型抽取和 Agent 接入提供真实评测数据；
- 避免以后更换模型时重新做人肉数据工程。

---

## 11. P0、P1、P2 分别会带来什么

### 11.1 P0：先让知识能够被可靠录入和查询

内部可看到：

- 上传和文档库；
- PDF 审核工作台；
- 实体解析；
- 结构化知识记录；
- 精确查询和全文搜索；
- 评测与审计。

主要价值：

> 建立第一条真正可运营、可审核、可追溯的数据生产线。

### 11.2 P1：让系统具备规模化抽取和更强检索

新增：

- pin、电气极限、时序、去耦、时钟和复位等专用 Schema；
- 模型辅助结构化抽取；
- 新旧 revision 差异；
- 冲突中心；
- hybrid retrieval；
- KnowledgeSnapshot；
- PcbCore 元件映射；
- 检索回归看板。

主要价值：

> 从“人工整理知识”升级为“机器辅助、人工把关、可规模化积累”。

### 11.3 P2：进入 PCB Agent 的设计闭环

新增：

- 板厂能力 profile；
- 内部规则和 waiver；
- design review 决策；
- 生命周期和替代料；
- 成功与失败案例；
- BoardPatch 与 ValidationReport 回写；
- 相似案例检索；
- MCP 适配；
- 文档更新影响到项目的报告。

主要价值：

> 让知识真正进入“查询—建议—修改—验证—审批—沉淀”的 Agent 全流程。

---

## 12. 第一批内部工作会是什么

建议首批数据控制在可审核范围内：

```text
20–30 个常用 IC
├── 当前 datasheet
├── 前一个 datasheet revision
├── 相关 application note
├── reference design
└── PCN / lifecycle 文档

2–3 份板厂能力文件
10 条内部设计规则
10 个历史 design review
10 个 approved waiver
100 个 golden extraction/retrieval cases
```

首批工作的目标不是追求数据量，而是验证：

- 能否正确区分相似 MPN；
- 能否区分同一 MPN 的不同 package；
- 能否区分新旧 revision；
- 能否区分绝对最大值和推荐工作条件；
- 能否保留条件、单位和适用范围；
- 能否在没有资料时明确返回 unknown；
- 能否保证 waiver 和项目机密不跨范围泄漏；
- 能否从软件中的结果跳回准确原文。

---

## 13. 常见误解

### 13.1 “这就是一个 PCB 文档聊天机器人”

不是。

聊天可以作为后续解释入口，但底层核心是：

```text
证据库
+ 版本化工程知识库
+ 审核工作台
+ 可解释检索服务
+ Agent typed context provider
+ 知识快照与评测系统
```

### 13.2 “搭 RAG 就是把 PDF 切块后放进向量数据库”

不是。

向量索引只是可重建的辅助检索手段。文档版本、实体、结构化知识、证据、审核、冲突和权限才是长期资产。

### 13.3 “以后模型足够强，就不需要人工审核”

至少在高风险 PCB 工程事实上不是这样。

模型可以减少录入和查找成本，但 pin、电气极限、电源时序、板厂硬限制、waiver 和替代兼容性仍需要明确的工程审核边界。

### 13.4 “PcbKnowledge 可以代替 DRC”

不能。

它能说明规则和建议来自哪里，但几何是否合法、连接是否正确、修改是否通过验证，仍由 PcbCore 和专用引擎决定。

### 13.5 “所有 PDF 都会发给外部大模型”

不会。

每份资料都有许可证和数据分类策略。内部、项目机密或禁止 AI 处理的资料，可以限制为人工查看，或仅允许在受控本地环境中处理。

### 13.6 “只要历史项目曾经这样做，Agent 就可以直接复用”

不能。

历史案例是参考，不是事实。Agent 必须重新检查当前器件、板层、规则、项目范围和验证结果。

### 13.7 “知识平台离线后 PCB 软件就不能用了”

不会。

PcbCore 不依赖 PcbKnowledge。离线时只失去知识查询和智能建议，基础设计和验证功能仍然工作。

---

## 14. 怎样判断这项工作是否成功

这项工作的成功不应只看“上传了多少 PDF”或“搜索结果看起来多智能”。更重要的是：

### 数据质量

- 已发布记录是否都有来源、revision 和 EvidenceAnchor；
- 单位、条件、封装和适用范围是否完整；
- 冲突是否被发现而不是被覆盖；
- 未知内容是否能正确保持 unknown。

### 工程可靠性

- 软件是否会把相似 MPN 或不同 package 混淆；
- Agent 建议是否都能追溯到知识记录和原文；
- 正式任务是否能够用 KnowledgeSnapshot 复现；
- PcbKnowledge 不可用时是否不影响 PcbCore 基础功能。

### 工作效率

- 测试人员整理一份资料所需时间是否下降；
- 工程师是否能更快完成高风险内容审核；
- 设计人员是否减少重复查文档和寻找历史结论；
- 新 revision 或 PCN 到来后，是否能快速定位潜在影响。

### 安全与治理

- 项目机密和 waiver 是否严格隔离；
- 模型和用户是否只能访问获授权资料；
- 发布、修改、查询和导出是否都有审计；
- 付费或禁止 AI 处理的资料是否按策略受控。

### Agent 价值

- Agent 是否减少无依据猜测；
- 是否能够明确指出冲突和缺失信息；
- 是否形成“知识—修改—验证—人工决定”的闭环；
- 成功和失败结果是否能沉淀为后续可检索案例。

---

## 15. 最终理解

`PcbKnowledge` 的长期价值不是拥有一个更大的文档库，而是形成一套持续积累的 PCB 工程知识生产机制：

```text
原始资料进入
→ 形成有证据的结构化知识
→ 在软件和 Agent 中被精确使用
→ 设计修改经过确定性验证和人工审批
→ 成功与失败结果重新沉淀为工程案例
→ 新资料到来后能够分析历史影响
```

因此，它既是后台数据平台，也是 PCB Agent 的知识基础设施；既服务自动化，也服务人工设计和审查。

最终应始终保持以下边界：

```text
PcbKnowledge 负责让系统“知道应该参考什么”
PcbAgentHarness 负责决定“下一步准备做什么”
PcbCore 负责验证“当前设计和修改是否成立”
电子工程师负责批准“高风险决策是否可以采用”
```

当这四部分组合起来后，PCB AI 才不只是生成建议，而是能够给出来源、接受验证、支持复现，并在长期使用中积累真正属于团队的工程能力。

---

## 附录 A：简要术语

| 术语 | 通俗解释 |
|---|---|
| Evidence Vault | 保存原始资料且不允许被静默替换的文件库 |
| EvidenceAnchor | 一条知识在原文中的具体页码和区域 |
| KnowledgeRecord | 一项稳定的知识对象，例如“某器件 VIN 推荐范围” |
| KnowledgeRecordVersion | 该知识在某个时间和文档版本下的具体内容 |
| KnowledgeSnapshot | 某次任务实际使用的知识版本清单 |
| Typed API | 按器件、pin、参数、规则等明确字段查询，而不是自由聊天 |
| EvidenceBundle | 一次查询返回的事实、建议、冲突、缺失信息和原文证据集合 |
| Curator | 负责资料录入、整理和证据核验的测试/数据人员 |
| Domain Reviewer | 负责高风险工程内容审核的电子工程师 |
| BoardContext | PcbCore 提供给 Agent 的当前板卡精确上下文摘要 |
| BoardPatch | Agent 或用户提出、可审核和验证的一组板卡修改 |
| ValidationReport | PcbCore 或专用引擎对修改前后结果给出的验证报告 |
| Waiver | 在明确项目、版本、规则和范围内批准的例外 |
| Golden Case | 用来持续验证抽取、检索或 Agent 是否退化的标准测试用例 |

