# Knowledge schemas

This directory is the versioned home for PcbKnowledge fact, rule, decision,
waiver, case, and lifecycle schemas. M0 intentionally defines no domain schema:
the first executable schemas arrive with their owning milestone and must include
validation fixtures, compatibility tests, and an explicit version.

Do not add free-form substitutes for a planned typed schema. A missing schema
must remain missing and fail closed.
